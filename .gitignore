# Python
__pycache__/
*.py[cod]
.env
venv/
.venv/

# OS
.DS_Store

# Certificates — binary key formats stay out of git
# The BankID test .pem files (certs/*.pem) are tracked intentionally:
# they are BankID's publicly distributed test-environment files, not secret production keys.
*.pfx
*.p12

# Google credentials
services/translation/credentials/
token.json
client_secret*.json
.coverage
htmlcov/
coverage/
server.log
server.err
.runtime_deps/
run_server.ps1

# Claude Code
.claude/
